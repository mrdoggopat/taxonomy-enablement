datadog:
  # include this if you are testing this locally
  apiKey: "<api key>"
  clusterName: "test-cluster"
  kubelet:
    tlsVerify: false
  # other configurations
  clusterChecks:
    enabled: true
clusterAgent:
  enabled: true
  confd:
    mysql.yaml: |-
      cluster_check: true
      init_config:
      instances:
        - host: "mydb.abcdef123456.us-east-1.rds.amazonaws.com"
          port: 3306
          user: datadog
          password: "123"
